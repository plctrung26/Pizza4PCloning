import { useEffect } from "react";

const Delivery = () => {
    useEffect(() => {
        console.log("hello")
    })
    return (
        <div>
            Hello World
        </div>
    );
};

export default Delivery;
